export interface ILoginFeatures {
    profileActions: {
        enabled: boolean
        edit: {
            enabled: boolean
        }
        remove: {
            enabled: boolean
        }
        viewInFiles: {
            enabled: boolean
        }
    }
}
